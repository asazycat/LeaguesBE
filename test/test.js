
import assert from 'assert' 

describe('All API Endpoints for LeaguesBE', () => {
    describe('/', () => {
        it('this should return the list of endpoints that LeaguesBE have available',() => {
            
        })
    })
})